<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="XFPINLoginSample.Views.ChangePINPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
    xmlns:pinview="clr-namespace:XFPINView;assembly=XFPINView"
    xmlns:vm="clr-namespace:XFPINLoginSample.ViewModels"
    Title="{Binding Title}"
    ios:Page.UseSafeArea="true">
    <ContentPage.BindingContext>
        <vm:ChangePINPageViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <Grid Margin="20,40,20,20" VerticalOptions="FillAndExpand">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--  Existing PIN Entry Box  -->
            <StackLayout Grid.Row="0" Spacing="15">
                <Label HorizontalTextAlignment="Center" Text="Enter exsiting PIN" />
                <pinview:PINEntryControl
                    x:Name="existingPINEntryControl"
                    PINValue="{Binding ExistingPIN}"
                    Style="{StaticResource PINEntryControlStyle}" />
            </StackLayout>

            <!--  New PIN Entry Box  -->
            <StackLayout
                Grid.Row="1"
                Margin="0,40,0,0"
                Spacing="15">
                <Label HorizontalTextAlignment="Center" Text="Enter New PIN" />
                <pinview:PINEntryControl
                    x:Name="newPINEntryControl"
                    PINValue="{Binding NewPIN}"
                    Style="{StaticResource PINEntryControlStyle}" />
            </StackLayout>

            <!--  Confirm PIN Entry Box  -->
            <StackLayout
                Grid.Row="2"
                Margin="0,40,0,0"
                Spacing="15">
                <Label HorizontalTextAlignment="Center" Text="Confirm PIN" />
                <pinview:PINEntryControl
                    x:Name="confirmPINEntryControl"
                    AutoDismissKeyboard="True"
                    PINValue="{Binding ConfirmPIN}"
                    Style="{StaticResource PINEntryControlStyle}" />
            </StackLayout>

            <!--  SAVE Button  -->
            <Button
                Grid.Row="3"
                Command="{Binding DoneCommand}"
                Style="{StaticResource ButtonStyle}"
                Text="DONE"
                VerticalOptions="End" />
        </Grid>
    </ContentPage.Content>
</ContentPage>